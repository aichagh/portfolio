const dig = 24;
const trad = 0;
const comic = 3;

function add_images() {
    add_dig();
    // add_trad();
    // add_comic();
}

function add_dig() {
    for(let i = 1; i <= dig; i++) {
        
    }
}